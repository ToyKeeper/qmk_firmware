# Compile options for ToyKeeper's Whitefox layout

# enable non-default features
DYNAMIC_MACRO_ENABLE = yes

# these seem to be enabled by default,
# but they're used in this layout, so turn them on anyway
# (commenting these out should make no difference to the build)
NKRO_ENABLE        = yes
MOUSEKEY_ENABLE    = yes
SPACE_CADET_ENABLE = yes

# I have the kono.store version with no LEDs
BACKLIGHT_ENABLE   = no

# this causes a compiler error when enabled
VISUALIZER_ENABLE  = no
